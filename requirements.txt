# Context-Sync Curator — Streamlit Community Cloud profile (2nd deployment)
# Python 3.10+

streamlit>=1.30
rdflib>=7.0
pandas>=2.0
plotly>=5.18

# Rule validation + optional OpenAI extraction
pydantic>=2.0
python-dotenv>=1.0
openai>=1.40

# HTML/article fetch helpers
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# Dense RAG / local sLLM dependencies are intentionally excluded from this cloud profile.
# For local Dense RAG or Hugging Face sLLM inference, install requirements-sllm.txt instead.
